services:
  medialytics:
    image: ghcr.io/drewpeifer/medialytics:latest
    container_name: medialytics
    # No environment variables needed - OAuth handles authentication and server discovery
    ports:
      - "8088:80"
